<h1>All restaurants</h1>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li>
      <h2><%= restaurant.name %></h2>

    </li>
  <% end %>
</ul>
<%= link_to 'More', restaurant_path(@restaurant), class: "btn btn-primary" %>
